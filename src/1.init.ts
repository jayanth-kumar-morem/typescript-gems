// Declare a variable with a type annotation.
// This ensures that the variable can only hold a string value.
var helloworld: string = "Hello World";

// Log the value of the variable to the console.
console.log(helloworld);
